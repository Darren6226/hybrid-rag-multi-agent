# LangChain 生态（锁定版本，避免兼容性问题）
langchain>=1.3.4
langchain-core>=1.4.0
langchain-experimental>=0.4.2   # ⚠️ 已标记 sunset，SemanticChunker/PythonREPL/LLMGraphTransformer 仍可用
langchain-community>=0.4.2      # ⚠️ 已标记 sunset，Neo4jGraph/GraphCypherQAChain 仍可用
langchain-neo4j>=0.9.0          # ✅ Neo4j 独立包（替代 langchain-community 中的 Neo4j 组件）
langchain-milvus>=0.3.3
langgraph>=1.2.4

# 数据库 & 数据验证
sqlalchemy>=2.0.0
pymysql>=1.1.0
pydantic>=2.0.0

# LLM 服务
dashscope>=1.14.0
openai>=1.0.0               # SenseNova DeepSeek API（OpenAI 兼容）

# 向量数据库 & 图数据库
pymilvus>=2.3.0
neo4j>=5.0.0

# 环境配置
python-dotenv>=1.0.0

# 安全沙箱
RestrictedPython>=7.0

# PDF 解析
PyMuPDF>=1.23.0
pdfplumber>=0.10.0
pdfminer.six>=20221105
PyPDF2>=3.0.0
Pillow>=10.0.0

# RAGAS 评估框架
ragas>=0.4.0
datasets>=4.0.0

# API 服务
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# 测试
pytest>=8.0.0
httpx>=0.27.0
